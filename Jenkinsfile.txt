pipeline{
agent any
environment {
ANYPOINT = credentials('ANYPOINT')
}
stages {
   stage ('Build') {
    steps {
	  sh 'mvn clean package'
	  }
	  }
	  stage('Deploy CloudHub') {
	  steps {
	  sh 'mvn clean deploy -P cloudhub -Danypoint.username=muditatiwari -Danypoint.password=Ashish@45'
	  }
	  }
	  }
	  }
